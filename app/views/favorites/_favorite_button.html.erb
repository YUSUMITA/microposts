<% if current_user.favoring?(micropost) %>
    <%= form_with(model: current_user.favorites.find_by(micropost_id: micropost.id), local: true, method: :delete) do |f| %>
        <%= hidden_field_tag :favorite_id, micropost.id %>
        <%= f.submit "Unfavor", class: "btn btn-danger btn-sm offset-sm-3" %>
    <% end %>
<% else %>
    <%= form_with(model: current_user.favorites.build, local: true) do |f| %>
        <%= hidden_field_tag :favorite_id, micropost.id %>
        <%= f.submit "Favor", class: "btn btn-primary btn-sm offset-sm-3" %>
    <% end %>
<% end %>